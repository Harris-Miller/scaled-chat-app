import { Grid, Typography } from '@mui/material';
import type { FC } from 'react';

export const Encounters: FC = () => {
  return (
    <Grid container>
      <Grid size={12}>
        <Typography variant="h1">Encounters</Typography>
      </Grid>
    </Grid>
  );
};
