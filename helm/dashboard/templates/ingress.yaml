# apiVersion: networking.k8s.io/v1
# kind: Ingress
# metadata:
#   name: kubernetes-dashboard-kong-ingress
# spec:
#   ingressClassName: nginx
#   rules:
#     - host: localhost
#       http:
#         paths:
#           - path: /
#             pathType: Prefix
#             backend:
#               service:
#                 name: kubernetes-dashboard-kong-proxy
#                 port:
#                   number: 443
